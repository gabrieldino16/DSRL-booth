PySide6>=6.6
Pillow>=10.2
# Entrega: QR de descarga, subida a SmugMug y envío por email.
qrcode[pil]>=7.4
requests>=2.31
requests-oauthlib>=1.3
# Opcional: solo para la fotocabina en vivo con webcam USB.
# Sin esto, el modo fotocabina usa una cámara simulada para probar el flujo.
opencv-python>=4.9
